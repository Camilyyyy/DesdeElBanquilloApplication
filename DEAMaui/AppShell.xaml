<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DEAMaui.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DEAMaui"
    xmlns:views="clr-namespace:DEAMaui.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="DEAMaui">

    <TabBar>
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate views:HomePage}"
            Route="HomePage" />

            <ShellContent
            Title="Países"
            ContentTemplate="{DataTemplate views:CountriesPage}"
            Route="CountriesPage" />
        
        <ShellContent
            Title="Posiciones"
            Icon="positions_icon.svg" 
            ContentTemplate="{DataTemplate views:PositionsPage}"
            Route="PositionsPage" />

        <ShellContent
            Title="Federaciones"
            ContentTemplate="{DataTemplate views:FederationsPage}"
            Route="FederationsPage" />

        <ShellContent
            Title="Ligas"
            ContentTemplate="{DataTemplate views:LeaguesPage}"
            Route="LeaguesPage" />

        <ShellContent
            Title="Jugadores"
            ContentTemplate="{DataTemplate views:PlayersPage}"
            Route="PlayersPage" />

        <ShellContent
            Title="Equipos"
            ContentTemplate="{DataTemplate views:TeamsPage}"
            Route="TeamsPage" />

    </TabBar>

</Shell>